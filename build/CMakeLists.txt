cmake_minimum_required(VERSION 3.30)
project(Imageborder)
set(CMAKE_CXX_COMPILER /usr/bin/clang++)
find_package( OpenCV REQUIRED )
include_directories( ${OpenCV_INCLUDE_DIRS} )
# message(CHECK_START "dirs are: ${OpenCV_INCLUDE_DIRS}")
add_executable( img_borders ../src/main.cpp )
target_link_libraries( img_borders ${OpenCV_LIBS} )
# message(CHECK_START "libs are: ${OpenCV_LIBS}")
